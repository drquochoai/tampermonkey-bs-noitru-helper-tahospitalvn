---
mode: 'agent'
---
Integrate Browserify into the project to bundle JavaScript files for use in a browser environment. Ensure that the build process is configured to handle dependencies correctly and that the output is optimized for performance.
The source code is in the `src` directory, and the output should be placed in the `dist` directory. The project uses npm for package management.
The file to split is "bs-noitru.fetch.user.js" for Tampermonkey.
