// common/utils.js
// Các hàm tiện ích dùng chung cho mọi domain
(function() {
    if (window.Utils) return; // Tránh nạp lại
    window.Utils = {
        // Thêm các hàm tiện ích tại đây (formatDate, calculateAge, ...)
        // Ví dụ:
        // calculateAge: function(dateString) { ... },
        // formatDate: function(date) { ... },
    };
})();
